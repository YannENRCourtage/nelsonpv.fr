rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // PDFs folder - authenticated users can read/write
    match /pdfs/{contactId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
    
    
    // Avatars folder - all authenticated users can read, users can write/delete their own
    match /avatars/{userId}/{fileName} {
       allow read: if request.auth != null;
       allow write, delete: if request.auth != null && request.auth.uid == userId;
    }

    // Allow users to upload project captures and photos
    match /projects/{projectId}/captures/{captureId} {
      allow read, write: if request.auth != null;
    }
    match /projects/{projectId}/photos/{photoId} {
      allow read, write: if request.auth != null;
    }
  }
}
